<div class="about">
  <h1><%= @user.name %>さんのフォローリスト</h1>
</div>

<div id="follow_list">
  <ul>
    <% @users.each do |user| %>
      <li><%= link_to user.name, user_path(user) %></li>
    <% end %>
  </ul>
</div>
